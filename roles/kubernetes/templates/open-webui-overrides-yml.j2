ollama:
  enabled: false

tika:
  enabled: true

ollamaUrls:
- http://ollama.ollama.{{ ollama_cluster_name }}.{{ ollama_tld }}

ingress:
  enabled: true
  host: "open-webui.open-webui.{{ open_webui_cluster_name }}.{{ open_webui_tld }}"
 
service:
  type: LoadBalancer

image:
  tag: "{{ open_webui_helm_image_tag }}"
